# GitHub token used only by the server to dispatch the private build workflow.
# Prefer a fine-grained token with Actions: Read and write access for this repository.
GITHUB_TOKEN=github_token_here

# Repository containing .github/workflows/build.yml
GITHUB_REPO=aishervin/URL2APK

# Optional HTTP port
PORT=3000
